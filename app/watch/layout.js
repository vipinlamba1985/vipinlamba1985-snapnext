export const metadata = {
  title: 'Family Story — SnapNext',
  description: 'Temporary private screen for watching a SnapNext family story together.',
  robots: { index: false, follow: false, noarchive: true },
};

export default function FamilyWatchLayout({ children }) {
  return children;
}
