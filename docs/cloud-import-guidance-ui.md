Cloud Sync UI guidance is implemented in `app/(app)/imports/page.js`.

Customer promise:
- Select up to 500 files for one import.
- SnapNext automatically processes 10 files at a time.
- The customer does not need to divide the selection manually.
- Live progress identifies the current automatic batch and total batches.
- Completed files remain safe if a later batch has a problem.
